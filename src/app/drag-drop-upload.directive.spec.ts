import { DragDropUploadDirective } from './drag-drop-upload.directive';

describe('DragDropUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDropUploadDirective();
    expect(directive).toBeTruthy();
  });
});
